Running the tool and understanding the results
==============================================

With your bundle and settings file ready, one command builds the index. This
page walks through running that command, reading the messages it prints, and
understanding the two files it leaves behind: the ``index.csv`` table and its
matching label. It also explains how the output files are named and what a
successful run looks like.
