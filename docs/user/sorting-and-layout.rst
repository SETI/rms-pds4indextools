Sorting and layout of the table
===============================

Once your columns are chosen, you can control the order of the rows and the
shape of the table. You can sort by one or more columns, choose between a
comma-separated and a fixed-width layout, and pick the line ending used
between records. This page explains each of these layout choices and shows how
they change the finished ``index.csv``.
