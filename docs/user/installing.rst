Installing the tool
===================

The tool is published on PyPI and installs a single command,
``pds4_create_xml_index``, that you run from a terminal. This page walks
through installing it, confirming that the command is on your path, and
knowing which versions of Python are supported. Once the command reports its
version, you are ready to build your first index.
